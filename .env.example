# Copy to .env and fill in.
# Get this from: Discord channel → Edit Channel → Integrations → Webhooks → New Webhook → Copy URL
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/REPLACE_ME

# Optional: account size for position sizing math (USD). 1% risk is computed from this.
ACCOUNT_SIZE_USD=10000
